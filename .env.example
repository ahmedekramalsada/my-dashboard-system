# =============================================================================
# .env — Copy this file to .env and fill in ALL values before starting.
# NEVER commit the real .env to git.
# =============================================================================

# ---- Domain ----------------------------------------------------------------
# Your public domain (e.g. myplatform.com) or leave as 127.0.0.1.nip.io for local dev
DOMAIN=127.0.0.1.nip.io

# ---- PostgreSQL Root Credentials -------------------------------------------
# These credentials are used by the control plane to create per-tenant DBs.
# Change BOTH before first launch. Use a long random string.
DB_ROOT_USER=root
DB_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
DB_ROOT_NAME=defaultdb

# ---- Super Admin Dashboard -------------------------------------------------
# HTTP Basic Auth for the /superadmin dashboard.
# Changing these requires rebuilding the saas-dashboard image.
DASHBOARD_USER=admin
DASHBOARD_PASS=CHANGE_ME_DASHBOARD_PASSWORD

# ---- Control Plane API Key -------------------------------------------------
# All write operations (create/delete/seed) require this header:
#   X-API-Key: <value>
# Generate with: openssl rand -hex 32
API_KEY=CHANGE_ME_GENERATE_WITH_openssl_rand_hex_32

# ---- CORS ------------------------------------------------------------------
# Comma-separated list of allowed origins for the API.
# In production, set this to your exact dashboard domain.
# Example: http://superadmin.myplatform.com
CORS_ORIGINS=*

# ---- Let's Encrypt TLS (optional) -----------------------------------------
# Set your email to enable automatic HTTPS via Let's Encrypt.
# Leave blank to stay on HTTP (local dev only).
LETS_ENCRYPT_EMAIL=
